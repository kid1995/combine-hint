apiVersion: security.istio.io/v1beta1
kind: AuthorizationPolicy
metadata:
  name: {{ .Values.prefix }}-authpolicy-allow-from-dboperator
spec:
  action: ALLOW
  rules:
    - from:
        - source:
            namespaces: ["dbops-operator"]