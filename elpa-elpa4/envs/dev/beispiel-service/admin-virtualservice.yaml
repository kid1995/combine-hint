apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: beispiel-service-admin
spec:
  hosts:
    - "dev-beispiel-service-admin-elpa-elpa4.copsinop.de"           # extern FQDN
    - "dev-beispiel-service-admin.elpa-elpa4.svc.cluster.local"     # intern FQDN
  gateways:
    - istio-system/mesh-gateway
    - mesh
  http:
    - route:
        - destination:
            host: dev-beispiel-service
            port:
              number: 8081
