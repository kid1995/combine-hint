apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: dest-rule
spec:
  host: tst-service-hint
  trafficPolicy:
    portLevelSettings:
      - port:
          number: 8080
        tls:
          mode: DISABLE
