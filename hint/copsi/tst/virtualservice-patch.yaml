apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: virtualservice
spec:
  hosts:
    - "tst-hint-elpa-elpa4.copsinop.de"
    - "tst-hint.elpa-elpa4.svc.cluster.local"
  gateways:
    - istio-system/mesh-gateway
    - mesh
  http:
    - name: tst-hint-route
      route:
        - destination:
            host: tst-hint-service-host
            port:
              number: 8080
