apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: virtualservice-metrics
spec:
  hosts:
    - "tst-hint-metrics-elpa-elpa4.copsinop.de"
    - "tst-hint-metrics.elpa-elpa4.svc.cluster.local"
  gateways:
    - istio-system/mesh-gateway
    - mesh
  http:
    - name: tst-hint-metrics-route
      route:
        - destination:
            host: tst-hint-metrics-host
            port:
              number: 8081
