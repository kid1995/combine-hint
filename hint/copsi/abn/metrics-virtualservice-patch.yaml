apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: virtual-service-metrics
spec:
  hosts:
    - "abn-hint-metrics-elpa-elpa4.copsinop.de"
    - "abn-hint-metrics.elpa-elpa4.svc.cluster.local"
  gateways:
    - istio-system/mesh-gateway
    - mesh
  http:
    - name: abn-metrics-route-hint
      route:
        - destination:
            host: abn-metrics-hint
            port:
              number: 8081
