apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: virtualservice
spec:
  hosts:
    - "prod-hint-elpa-elpa4.copsinop.de"
    - "prod-hint.elpa-elpa4.svc.cluster.local"
  gateways:
    - istio-system/mesh-gateway
    - mesh
  http:
    - name: prod-hint-route
      route:
        - destination:
            host: prod-hint-service-host
            port:
              number: 8080
