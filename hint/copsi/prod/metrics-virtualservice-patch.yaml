apiVersion: networking.istio.io/v1beta1
kind: VirtualService
metadata:
  name: virtual-service-metrics
spec:
  hosts:
    - "prod-hint-metrics-elpa-elpa4.copsinop.de"
    - "prod-hint-metrics.elpa-elpa4.svc.cluster.local"
  gateways:
    - istio-system/mesh-gateway
    - mesh
  http:
    - name: prod-metrics-route-hint
      route:
        - destination:
            host: prod-metrics-hint
            port:
              number: 8081
