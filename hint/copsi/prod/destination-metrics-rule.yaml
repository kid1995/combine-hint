apiVersion: networking.istio.io/v1beta1
kind: DestinationRule
metadata:
  name: hint-dest-rule-metrics
spec:
  host: prod-hint-metrics-host
  trafficPolicy:
    portLevelSettings:
      - port:
          number: 8081
        tls:
          mode: DISABLE
