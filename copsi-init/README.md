# init script

``` shell
git clone ssh://git@git.system.local:7999/elpa/copsi-init.git &&\
cd copsi-init &&\
chmod +x init-copsi.sh &&\
./init-copsi.sh &&\
mv copsi ../ &&\
cd .. &&\
rm -rf copsi-init
```
